from setuptools import find_packages, setup
#from package import Package

setup(name='mysql-permissions-exporter',
      version='0.1',
      description='Export the status of permissions that are assigned to users',
      url='',
      author='Ecobee',
      author_email='',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)